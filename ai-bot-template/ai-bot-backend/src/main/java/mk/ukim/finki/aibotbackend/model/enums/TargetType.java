package mk.ukim.finki.aibotbackend.model.enums;

/**
 * What an {@code ExtractionTarget} points at within the social network.
 */
public enum TargetType {
    PROFILE,
    HASHTAG,
    KEYWORD,
    FEED_URL
}
